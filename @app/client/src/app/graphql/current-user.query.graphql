query currentUser {
  currentUser {
    username,
    notificationFrequency,
    pushNotification,
    emailNotification,
    profilePhoto,
    id,
    isSetup,
    isAdmin,
    watchLists {
      totalCount
    },
    pushSubscriptions {
      nodes {
        id
      }
    }
  }
}
