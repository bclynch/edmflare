mutation resetPassword( $userId: Int!, $token: String!, $newPassword: String!) {
  resetPassword(
    input: { userId: $userId, token: $token, newPassword: $newPassword }
  ) {
    user {
      username
    }
  }
}